{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}

  <div class="geolocation-field-wrapper">

    <div class="custom-control custom-radio custom-control-inline">
      <input type="radio" id="{{ id }}" name="{{ name }}" value="search" class="custom-control-input">
      <label class="custom-control-label" for="{{ id }}">Search</label>
    </div>
    <div class="custom-control custom-radio custom-control-inline">
      <input type="radio" id="{{ id }}" name="{{ name }}" value="latlong" class="custom-control-input">
      <label class="custom-control-label" for="{{ id }}">Latitude & Longitude</label>
    </div>

{#    {{ dump(geolocation_settings()) }}#}
  </div>

{% endblock %}